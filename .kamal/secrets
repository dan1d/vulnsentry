# Secrets defined here are available for reference under registry/password, env/secret, builder/secrets,
# and accessories/*/env/secret in config/deploy.yml. All secrets should be pulled from either
# password manager, ENV, or a file. DO NOT ENTER RAW CREDENTIALS HERE! This file needs to be safe for git.

# Improve security by using a password manager. Never check config/master.key into git!
RAILS_MASTER_KEY=$(cat config/master.key 2>/dev/null || echo "$RAILS_MASTER_KEY")

# Load secrets from .env.production file directly (more reliable than sourcing)
KAMAL_REGISTRY_PASSWORD=$(grep '^KAMAL_REGISTRY_PASSWORD=' .env.production 2>/dev/null | cut -d= -f2- | tr -d '"' || echo "$KAMAL_REGISTRY_PASSWORD")
GH_TOKEN=$(grep '^GH_TOKEN=' .env.production 2>/dev/null | cut -d= -f2- | tr -d '"' || echo "$GH_TOKEN")
PGPASSWORD=$(grep '^PGPASSWORD=' .env.production 2>/dev/null | cut -d= -f2- | tr -d '"' || echo "$PGPASSWORD")
POSTGRES_PASSWORD=$PGPASSWORD
DEEPSEEK_API_KEY=$(grep '^DEEPSEEK_API_KEY=' .env.production 2>/dev/null | cut -d= -f2- | tr -d '"' || echo "$DEEPSEEK_API_KEY")
ENABLE_DEEPSEEK_BUNDLED_GEMS_ASSISTANT=true
