<%= render "admin/shared/nav" %>

<section class="section">
  <div class="container">
    <nav class="level mb-5">
      <div class="level-left">
        <div class="level-item">
          <div>
            <h1 class="title is-4">Edit <%= @project.name %></h1>
            <p class="subtitle is-6 has-text-grey">Update project configuration.</p>
          </div>
        </div>
      </div>
    </nav>

    <%= render "form", project: @project %>
  </div>
</section>
