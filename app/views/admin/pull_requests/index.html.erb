<%= render "admin/shared/nav" %>

<div class="mx-auto max-w-6xl px-4 py-6 space-y-4">
  <div>
    <h1 class="text-2xl font-semibold">Pull requests</h1>
    <p class="text-sm text-gray-600">PRs created by this app (and tracked in DB).</p>
  </div>

  <div class="rounded border bg-white overflow-hidden">
    <table class="min-w-full text-sm">
      <thead class="bg-gray-50 text-gray-700">
        <tr>
          <th class="px-4 py-2 text-left">PR</th>
          <th class="px-4 py-2 text-left">Branch</th>
          <th class="px-4 py-2 text-left">Gem</th>
          <th class="px-4 py-2 text-left">Status</th>
          <th class="px-4 py-2"></th>
        </tr>
      </thead>
      <tbody class="divide-y">
        <% @pull_requests.each do |pr| %>
          <tr>
            <td class="px-4 py-2 font-mono"><%= pr.pr_number || "?" %></td>
            <td class="px-4 py-2 font-mono"><%= pr.candidate_bump.base_branch %></td>
            <td class="px-4 py-2"><%= pr.candidate_bump.gem_name %> <span class="font-mono">→ <%= pr.candidate_bump.target_version %></span></td>
            <td class="px-4 py-2"><%= pr.status %></td>
            <td class="px-4 py-2 text-right">
              <%= link_to "View", admin_pull_request_path(pr), class: "text-blue-600 hover:underline" %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
